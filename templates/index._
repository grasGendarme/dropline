<!DOCTYPE html>
<html>
<head>
	<meta charset=utf-8 />
	<title>Drop line.</title>
</head>
<body>
	<% _.each(messages, function(message) { %> <li class="message"><%=message%></li><% }); %>

	<p>Add your message: </p>
	<form action="/new" method="GET">
	<input type="text" name="content" value="...">
	<input type="submit" value="Envoyer!">
</form>
</body>
</html>
